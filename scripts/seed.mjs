#!/usr/bin/env node
console.log(`AI Lead Hunter demo clinics are created automatically the first time you open the dashboard.

There is no global seed into a shared database: each signed-in workspace gets its own 20 sample US dental clinics, marked as demo data.

Use Settings → Reset demo clinics to reload them.`);
